namespace Layout.Client
{
    /// <summary>
    /// Which authority owns Layout guides for the current world.
    /// </summary>
    public enum ClientAuthorityMode
    {
        Detecting,
        Networked,
        Local
    }
}
